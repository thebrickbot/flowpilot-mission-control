# ╔══════════════════════════════════════════════════════════════════╗
# ║  FlowPilot Mission Control — Frontend Environment               ║
# ║  Copy to .env.local and fill in your values                     ║
# ╚══════════════════════════════════════════════════════════════════╝

# ── API connection ───────────────────────────────────────────────────────────
# REQUIRED: base URL for browser → backend calls.
# Must be reachable from the user's browser (not Docker-internal hostnames).
# Atlas local:   http://localhost:8000
# LAN / Tailscale: http://192.168.1.50:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# ── Auth mode ────────────────────────────────────────────────────────────────
# local  → shared bearer token entered in the UI (recommended for Atlas)
# clerk  → Clerk.dev SSO (cloud/multi-tenant)
NEXT_PUBLIC_AUTH_MODE=local

# ── Clerk auth (only used when NEXT_PUBLIC_AUTH_MODE=clerk) ──────────────────
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/boards
NEXT_PUBLIC_CLERK_AFTER_SIGN_OUT_URL=/

# ── FlowPilot branding ────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME=FlowPilot Mission Control
